<%= form_for(@user_with_hall) do |f| %>
  <% if @user_with_hall.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user_with_hall.errors.count, "error") %> prohibited this user_with_hall from being saved:</h2>

      <ul>
      <% @user_with_hall.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :email %><br>
    <%= f.text_field :email %>
  </div>
  <div class="field">
    <%= f.label :Hall_ID %><br>
    <%= f.number_field :Hall_ID %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
